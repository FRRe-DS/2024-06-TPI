{
  "scripts": {
    "start": "concurrently \"cd service && npm install && node backend.js\" \"cd client/src && npm install && npm run build\""
  },

  "devDependencies": {
    "concurrently": "^7.0.0",
    "vite": "^4.0.0"
  },
  
  "type": "module",  
  
  "dependencies": {
    "@sveltejs/adapter-auto": "^3.2.5",
    "dotenv": "^16.4.5",
    "mysql": "^2.18.1", 
    "mysql2": "^3.11.3",  
    "express": "^4.18.2"  
  }
}